import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

// Adds the main App component to the root element of the document
ReactDOM.render(<App />, document.getElementById('root'))
